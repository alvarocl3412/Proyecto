<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permitir tráfico en texto claro para el dominio localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- IP del host para el emulador -->
    </domain-config>
</network-security-config>